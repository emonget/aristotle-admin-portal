# digest-admin-portal
interact with movie reviews stored in database

## How to
- install deps 
```shell
npm i
```
- copy [env template](.env.local.example) to `.env.local` and customize placeholder values
